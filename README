Befor running this run the pox server as
"sudo python pox.py forwarding.l2_multi openflow.discovery --eat-early-packets openflow.spanning_tree --no-flood --hold-down"

then run 
"sudo python vishnu_s.py"

give the number of switches and hosts when asked

wait for 10 seconds before testing pingall for network discovery